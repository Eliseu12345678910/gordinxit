import type { DeviceType } from '@/types/chat'

export const videoFiles: Record<DeviceType, string> = {
  android: '/video/video-android-ios.mp4',
  ios: '/video/video-android-ios.mp4',
  emulator: '/video/video-emulador.mp4',
}
